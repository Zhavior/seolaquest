import { QuestRoutePending } from '@/components/quest'

/** Basecamp Settings route fallback: stacked configuration scrolls. */
export default function SettingsLoading() {
  return <QuestRoutePending label="Opening Basecamp Settings" />
}
