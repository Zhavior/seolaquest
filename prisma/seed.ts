// SEOlaQuest intentionally ships without a seed command.
// Production data belongs to authenticated users and must not be erased or replaced with demo leads.
console.log('No seed data created. Sign in and add your own tracking keyword instead.')
